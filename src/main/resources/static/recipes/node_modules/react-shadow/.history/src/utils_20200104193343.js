export function defineElement(name) {
    const [tag, extension, tagExtend] = parseName(name);
        window.customElements.define(
            tag,
            impl.base(extension, middleware),
            tagExtend && { extends: tagExtend }
        );
}

function parseName(name) {
    
}